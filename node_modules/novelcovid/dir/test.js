let covid = require('../index.js');

(async () => {
    let sortedStates = await covid.getCountry({sort: 'deaths'});
    sortedStates = await sortedStates.map(x=>x.deaths);
    console.log(sortedStates);
})();